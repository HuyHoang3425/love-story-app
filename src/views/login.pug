doctype html
html(lang="vi")
  head
    meta(charset="UTF-8")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    link(rel="stylesheet", href="/style.css")
    title Đăng nhập
      

  body
    .particles
      .particle(style="left: 20%; animation-delay: 0s;")
      .particle(style="left: 40%; animation-delay: 1s;")
      .particle(style="left: 60%; animation-delay: 2s;")
      .particle(style="left: 80%; animation-delay: 3s;")

    .login-container
      .login-header
        h2.login-title Đăng nhập
        p.login-subtitle Chào mừng trở lại

      form(method="POST", action="api/v1/auth/login")
        .form-group
          label.form-label(for="email") Email:
          input.form-input(type="email", name="email", id="email", required, placeholder="Nhập email của bạn")

        .form-group
          label.form-label(for="password") Mật khẩu:
          input.form-input(type="password", name="password", id="password", required, placeholder="Nhập mật khẩu")

        button.login-button(type="submit") Đăng nhập

      .forgot-password
        a(href="#") Quên mật khẩu?

      .divider
        span Hoặc đăng nhập bằng

      .social-login
        button.social-btn(type="button") 📧 Google
        button.social-btn(type="button") 📘 Facebook

      .security-note 🔒 Thông tin của bạn được bảo mật và an toàn
